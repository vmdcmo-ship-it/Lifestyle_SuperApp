// Placeholder - user-service không dùng Prisma, chỉ để Dockerfile.backend COPY không lỗi
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
